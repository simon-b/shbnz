remote_theme: mmistakes/minimal-mistakes
title: shb.nz
description: "Operations research and data science"
minimal_mistakes_skin: "default" # "air", "aqua", "contrast", "dark", "dirt", "neon", "mint", "plum" "sunrise"

author:
  name: "Simon Bull"
  bio: "Operations research and data science leader"
  location: "San Francisco"
  email: "simon@shb.nz"

include: ["_pages"]

defaults:
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true
      read_time: true
      comments: true
      share: true
      related: true
  # _pages
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: single
      author_profile: true

plugins:
  - jekyll-include-cache
